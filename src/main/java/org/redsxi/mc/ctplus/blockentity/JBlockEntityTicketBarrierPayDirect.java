package org.redsxi.mc.ctplus.blockentity;

import net.minecraft.core.BlockPos;
import net.minecraft.world.level.block.state.BlockState;

public class JBlockEntityTicketBarrierPayDirect extends BlockEntityTicketBarrierPayDirect {
    public JBlockEntityTicketBarrierPayDirect(BlockPos pos, BlockState state, boolean isTransitPlus) {
        super(pos, state, isTransitPlus);
    }
}
